# PyGDO To Do

Keep this list concrete and prioritized. A TODO is an intended piece of work,
not an idea to discuss.

## Now

- Complete the `gtranslate` module surface: decide command syntax, validate
  language arguments, render a useful translated response, and test it across
  CLI/web transports.
- Keep `WRITE_A_MODULE.md` accurate as module conventions evolve.

## Next

- Add provider-independent translation errors, rate-limit handling, and a
  documented provider boundary to `gtranslate`.
- Make new-module setup consistently include tests, language files, and safe
  dependency declarations.

## Maintenance

- Review this list regularly; finish, reprioritize, or remove stale items.
- Keep implementation history in Git rather than duplicating it here.
